<?php

namespace App\Models;

// We import the class 'Model', this class came with the installation of the
// illuminate/database component, installed through Composer CLI to
// administrate the connections and the petitions between the database and
// the framework.
use Illuminate\Database\Eloquent\Model;

class User extends Model
{
    //
}